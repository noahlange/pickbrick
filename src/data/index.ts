import type { LegoColor, LegoPart } from "../types";

import _colors from "./colors.json";
import _parts from "./parts.json";

export const colors: LegoColor[] = _colors;
export const parts: LegoPart[] = _parts;
